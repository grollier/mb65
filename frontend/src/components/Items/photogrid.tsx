import { Container, Flex, Grid, GridItem, Image } from "@chakra-ui/react";

import ImagesGridArray from "@/components/Common/dialog"

const PhotoGrid = () => {
	return (
		<Container fluid>
			<ImagesGridArray />
		</Container>
	);
};

export default PhotoGrid;
